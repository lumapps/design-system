```javascript import
import { Icon, Size, Theme } from 'LumX';
import { mdiEmail } from 'LumX/icons';
```


# Iconography

**Icons are small visual representations of common actions, objects or ideas. They help users to identify information and provide context.**

## Sizes
Icons come in 6 sizes: xxs, xs, s, m, l, xl.
```javascript jsx withThemeSwitcher
(theme) => (
    <>
        <Icon icon={mdiEmail} theme={theme} size={Size.xxs} />
        <Icon icon={mdiEmail} theme={theme} size={Size.xs} />
        <Icon icon={mdiEmail} theme={theme} size={Size.s} />
        <Icon icon={mdiEmail} theme={theme} size={Size.m} />
        <Icon icon={mdiEmail} theme={theme} size={Size.l} />
        <Icon icon={mdiEmail} theme={theme} size={Size.xl} />
</>
);
```

## Layout
We use <a href="https://material.io/archive/guidelines/style/icons.html" target="_blank">Material Design</a> grid and proportions.

```javascript jsx withThemeSwitcher
(theme) => (
    <>
        <img src="demo/react/doc/product/foundations/assets/iconography-layout.png" width="128" />
        <img src="demo/react/doc/product/foundations/assets/iconography-keylines.png" width="128" />
</>
);
```

## Library
Find library on <a href="https://materialdesignicons.com/" target="_blank">materialdesignicons.com</a>.

