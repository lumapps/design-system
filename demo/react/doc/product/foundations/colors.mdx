import { COLOR_VARIANTS } from 'LumX/demo/constants';
import { DemoColor } from 'LumX/demo/react/layout/DemoColor';
import { ColorPalette, Theme } from 'LumX';

# Colors

**Available color palette**

## Primary palette

### Blue

<DemoColor color={ColorPalette.blue} colorVariants={COLOR_VARIANTS} />

### Dark

<DemoColor color={ColorPalette.dark} colorVariants={COLOR_VARIANTS} />

### Light

<DemoColor color={ColorPalette.light} colorVariants={COLOR_VARIANTS} theme={Theme.dark} />

## Secondary palette

### Yellow

<DemoColor color={ColorPalette.yellow} colorVariants={COLOR_VARIANTS} />

### Red

<DemoColor color={ColorPalette.red} colorVariants={COLOR_VARIANTS} />

### Green

<DemoColor color={ColorPalette.green} colorVariants={COLOR_VARIANTS} />

