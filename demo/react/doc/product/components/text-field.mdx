```javascript import
import { ReactElement, useState } from 'react';
import { Chip, ChipGroup, TextField, Theme, IconButton, Emphasis, Size } from '@lumx/react';
import { mdiMagnify, mdiClose } from '@lumx/icons';
```

# Text field

**Text fields are simple text inputs.**

## Text field

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return <TextField label="Textfield label" value={value} onChange={setValue} theme={theme} />;
};
```

## Text area

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return <TextField label="Textfield label" value={value} onChange={setValue} theme={theme} type="textarea" />;
};
```

## States

There are three states: _disabled_, _valid_, and _invalid_.

### Disabled

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    return <TextField label="Textfield label" isDisabled={true} theme={theme} />;
};
```

### Valid

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('Valid value');

    return <TextField label="Textfield label" value={value} onChange={setValue} isValid={true} theme={theme} />;
};
```

### Invalid

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('Invalid value');

    return <TextField label="Textfield label" value={value} onChange={setValue} hasError={true} theme={theme} />;
};
```

## Options

There are six options: _helper text_, _placeholder_, _icon_, _clearable_ _with counter_ and _with chips_.

### Helper text

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return <TextField label="Textfield label" value={value} onChange={setValue} helper={'Helper text'} theme={theme} />;
};
```

### Placeholder

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return (
        <TextField
            label="Textfield label"
            value={value}
            onChange={setValue}
            placeholder={'Placeholder text'}
            theme={theme}
        />
    );
};
```

### Icon

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return <TextField value={value} onChange={setValue} icon={mdiMagnify} theme={theme} />;
};
```

### Clearable

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return <TextField isClearable value={value} onChange={setValue} theme={theme} />;
};
```

### With Counter

```javascript jsx withThemeSwitcher
() => (
    <p>Textfield avec compteur de caractère.</p>
);
```

### With Chips

```javascript jsx withThemeSwitcher disableGrid
(theme) => {
    const [value, setValue] = useState('');

    return (
        <TextField
            value={value}
            onChange={setValue}
            theme={theme}
            chips={
                <ChipGroup>
                    <Chip size={Size.s}>
                        First
                    </Chip>
                    <Chip size={Size.s}>
                        Second
                    </Chip>
                    <Chip size={Size.s}>
                        Third
                    </Chip>
                </ChipGroup>
            }
        />
    );
};
```

### Properties

<PropTable component="TextField" />
