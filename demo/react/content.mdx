import { Chip, ColorPalette, Icon, Size, Theme } from 'LumX';

# Hello, world!

<>
    <Chip theme={Theme.light}>
        Label
    </Chip>
    <Chip color={ColorPalette.red} theme={Theme.light} size={Size.s}>
        Label
    </Chip>
</>
