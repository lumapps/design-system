export function handleBasicClasses({
    color,
    size,
    theme,
    variant,
    prefix,
}: {
    color?: string;
    size?: string;
    theme?: string;
    variant?: string;
    prefix: string;
}): string;
