import { IconProps, IconSizes, Icon as UI } from '@lumx/core/js/components/Icon';

import Icon from './Icon.vue';

Icon.className = UI.className;

export { Icon };
export type { IconProps, IconSizes };
