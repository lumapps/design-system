# Dialog

## Default

Dialogs have a title, a body, and at least one or more buttons.

<DemoBlock demo="default" engine={props.engine} withThemeSwitcher />

## Prevent Auto dismiss

Prevent the clickaway and the escape to dismiss.

<DemoBlock demo="prevent-auto-dismiss" engine={props.engine} withThemeSwitcher />

## With scrollbar
When a dialog requires scrolling, the title stays at the top while the buttons stay at the bottom.

<DemoBlock demo="scrollbar" engine={props.engine} withThemeSwitcher />

### With header action

There can be actions in the dialog header.

<DemoBlock demo="action" engine={props.engine} withThemeSwitcher />

## Alert dialog

Use alert dialogs to interrupt users with urgent information, details, or action requests.

<DemoBlock demo="alert" engine={props.engine} withThemeSwitcher />

## Confirm dialog

Confirmation dialogs cannot be dismissed until users confirm a choice.

<DemoBlock demo="confirm" engine={props.engine} withThemeSwitcher />

## Sizes
Dialogs come in four sizes: tiny (400dp), regular (600dp), big (800dp), and huge (full screen).

<DemoBlock demo="sizes" engine={props.engine} withThemeSwitcher />


# Properties

<PropTable component="Dialog" />
