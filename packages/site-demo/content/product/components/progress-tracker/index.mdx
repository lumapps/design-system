---
frameworks: ['react']
---
import * as DemoDefault from './react/default.tsx';
import * as DemoStates from './react/states.tsx';
import * as DemoHelper from './react/helper.tsx';
import * as DemoError from './react/error.tsx';
import ReactProgressTracker from 'lumx-docs:@lumx/react/components/progress-tracker/ProgressTracker';
import ReactProgressTrackerStep from 'lumx-docs:@lumx/react/components/progress-tracker/ProgressTrackerStep';

# Progress tracker

**Progress tracker convey user progress through a sequence of steps.**

<DemoBlock demo={{ react: DemoDefault }} />

## States

A step can be active or complete.

<DemoBlock orientation="horizontal" demo={{ react: DemoStates }} />

### Helper

A step can be completed by a helper text.

<DemoBlock demo={{ react: DemoHelper }} />

### Error

When an error occurs, the error message can replace the helper text.
Step can be inactive or active but can't be completed while in error.

<DemoBlock orientation="horizontal" demo={{ react: DemoError }} />

### ProgressTracker properties

<PropTable docs={{ react: ReactProgressTracker }} />

### ProgressTrackerStep properties

<PropTable docs={{ react: ReactProgressTrackerStep }} />
