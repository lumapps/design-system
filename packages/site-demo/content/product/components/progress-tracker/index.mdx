# Progress tracker

**Progress tracker convey user progress through a sequence of steps.**
A step can be completed by a helper text.

## States

There are four states: _Current_, _Future_, _Completed_, and _Error_.

<DemoBlock2 demo="states" engine={props.engine} disableGrid />

### Current

<DemoBlock2 demo="current" engine={props.engine} disableGrid />

### Future

<DemoBlock2 demo="future" engine={props.engine} disableGrid />

### Completed

<DemoBlock2 demo="completed" engine={props.engine} disableGrid />

### Future with helper

<DemoBlock2 demo="helper" engine={props.engine} disableGrid />

### Error

When an error occurs, the error message can replace the helper text.

<DemoBlock2 demo="error" engine={props.engine} disableGrid />
