# Progress tracker

**Progress tracker convey user progress through a sequence of steps.**
A step can be completed by a helper text.

## States

There are four states: _Current_, _Future_, _Completed_, and _Error_.

<DemoBlock demo="states" engine={props.engine} disableGrid />

### Current

<DemoBlock demo="current" engine={props.engine} disableGrid />

### Future

<DemoBlock demo="future" engine={props.engine} disableGrid />

### Completed

<DemoBlock demo="completed" engine={props.engine} disableGrid />

### Future with helper

<DemoBlock demo="helper" engine={props.engine} disableGrid />

### Error

When an error occurs, the error message can replace the helper text.

<DemoBlock demo="error" engine={props.engine} disableGrid />
