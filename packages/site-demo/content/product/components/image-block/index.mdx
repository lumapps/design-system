---
frameworks: ['react', 'vue']
---
import * as ReactDemoDefault from './react/default.tsx';
import * as ReactDemoCustomPadding from './react/custom-padding.tsx';
import * as ReactDemoInfoAbove from './react/info-above.tsx';
import * as ReactDemoInfoAlignment from './react/info-alignment.tsx';
import * as VueDemoDefault from './vue/default.vue';
import * as VueDemoCustomPadding from './vue/custom-padding.vue';
import * as VueDemoInfoAbove from './vue/info-above.vue';
import * as VueDemoInfoAlignment from './vue/info-alignment.vue';
import ReactImageBlock from 'lumx-docs:@lumx/react/components/image-block/ImageBlock';
import VueImageBlock from 'lumx-docs:@lumx/vue/components/image-block/ImageBlock';

# Image block

**Image blocks display an image with related info.**
Image blocks support thumbnail's formats (see [thumbnail](/product/components/thumbnail)).

<DemoBlock withThemeSwitcher wrap={false} demo={{ react: ReactDemoDefault, vue: VueDemoDefault }} />

## Custom padding

A custom padding can be added.

<DemoBlock wrap={false} demo={{ react: ReactDemoCustomPadding, vue: VueDemoCustomPadding }} />

## Info above

Info can be placed above the image.

<DemoBlock withThemeSwitcher wrap={false} demo={{ react: ReactDemoInfoAbove, vue: VueDemoInfoAbove }} />

## Info alignment

There are three alignments: `left`, `center` and `right`.

<DemoBlock withThemeSwitcher wrap={false} demo={{ react: ReactDemoInfoAlignment, vue: VueDemoInfoAlignment }} />

### Accessibility concerns

Image blocks use standard HTML figure element with the `title`, `description` and `tags` displayed in the figure caption.

See [thumbnail accessibility concerns](/product/components/thumbnail/#accessibility-concerns) for the image.

### Properties

<PropTable docs={{ react: ReactImageBlock, vue: VueImageBlock }} />
