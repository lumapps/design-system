# Image-block

**Image blocks display an image with related info.**

## Default

Image blocks support thumbnail's different formats (see [thumbnail](/thumbnail)).

<DemoBlock2 demo="default" engine={props.engine} disableGrid withThemeSwitcher />

## Custom padding

A custom padding can be added.

<DemoBlock2 demo="image-block-padding" engine={props.engine} disableGrid withThemeSwitcher />

## Info over

Info can be placed over the image.

<DemoBlock2 demo="image-block-over" engine={props.engine} disableGrid withThemeSwitcher />

## Info alignment

There are three alignments: _left_, _center_ and, _right_.

<DemoBlock2 demo="image-block-alignment" engine={props.engine} disableGrid withThemeSwitcher />

### Properties

<PropTable component="ImageBlock" />
