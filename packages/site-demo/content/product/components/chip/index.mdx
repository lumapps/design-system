---
frameworks: ['react', 'vue']
---
import * as ReactDemo1 from './react/demo1.tsx';
import * as ReactDemo2 from './react/demo2.tsx';
import * as ReactDemo3 from './react/demo3.tsx';
import * as ReactDemo4 from './react/demo4.tsx';
import * as ReactDemo5 from './react/demo5.tsx';
import * as ReactDemo6 from './react/demo6.tsx';
import * as VueDemo1 from './vue/demo1.vue';
import * as VueDemo2 from './vue/demo2.vue';
import * as VueDemo3 from './vue/demo3.vue';
import * as VueDemo4 from './vue/demo4.vue';
import * as VueDemo5 from './vue/demo5.vue';
import * as VueDemo6 from './vue/demo6.vue';
import ReactChip from 'lumx-docs:@lumx/react/components/chip/Chip';
import ReactChipGroup from 'lumx-docs:@lumx/react/components/chip/ChipGroup';
import VueChip from 'lumx-docs:@lumx/vue/components/chip/Chip';
import VueChipGroup from 'lumx-docs:@lumx/vue/components/chip/ChipGroup';

# Chip

**Chips display distinct inputs, attributes, or actions.**

## Default

Use default to display distinct attributes such as tags on a blog post.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo1, vue: VueDemo1 }} />

## Dismissible

Use dismissible when chips can be removed by users.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo2, vue: VueDemo2 }} />

## Choice

Use choice when users make an exclusive choice between values. When space is not a constraint, use this chip instead of a select component.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo3, vue: VueDemo3 }} />

## Filter

Use filter as an entry point to one or several choices. When it is active, it can have a clear button.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo4, vue: VueDemo4 }} />

## Small

Use small when space is a constraint, as in areas like text fields, selects, dropdown menus and metadata.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo5, vue: VueDemo5 }} />

## Chip Group

Use chip groups to group chips together. Groups give chips responsive behavior.

<DemoBlock orientation="horizontal" withThemeSwitcher demo={{ react: ReactDemo6, vue: VueDemo6 }} />

### Accessibility concerns

Chips can be disabled in two ways:

-   `disabled`/`isDisabled` which completely disables the chip. It's not focusable and pointer events (hover, click,
    etc.) are all disabled too. You'll have to find another way to indicate to users that the chip is here and why it
    is disabled.
-   `aria-disabled` only disable changes on the chip (read only) so it's still focusable and accessible. You'll have
    to setup either a tooltip or an aria description (must be visible too) to explain to the user why it is disabled.

### Chip properties

<PropTable docs={{ react: ReactChip, vue: VueChip }} />

### ChipGroup properties

<PropTable docs={{ react: ReactChipGroup, vue: VueChipGroup }} />
