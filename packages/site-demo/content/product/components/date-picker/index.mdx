---
frameworks: ['react']
---
import * as DemoDefault from './react/default.tsx';
import * as DemoInput from './react/input.tsx';
import * as DemoRestrictedSelectionModes from './react/restricted-selection-modes.tsx';
import ReactDatePicker from 'lumx-docs:@lumx/react/components/date-picker/DatePicker';

# Date picker

**Date pickers allow users to pick a date from a calendar.**

<DemoBlock orientation="horizontal" demo={{ react: DemoDefault }} />

## Input

Date picker can be triggered by a click on a [text field](/product/components/text-field).

<DemoBlock orientation="horizontal" demo={{ react: DemoInput }} />

## Restricted selection modes

Date selection can be restricted via `minDate` and `maxDate` properties.

<DemoBlock orientation="horizontal" demo={{ react: DemoRestrictedSelectionModes }} />

### Properties

<PropTable docs={{ react: ReactDatePicker }} />
