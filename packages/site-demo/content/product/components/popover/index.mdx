# Popover

## Default behavior
Popover can be used to display elements above the rest of the UI.

<DemoBlock2 demo="default" engine={props.engine} withThemeSwitcher />

## Offset
Popover can be offseted from its anchor point.

<DemoBlock2 demo="offset" engine={props.engine} withThemeSwitcher />

## Control the popper visibility

<DemoBlock2 demo="visibility" engine={props.engine} withThemeSwitcher />

## Using anchor point
Popover can be placed using one of 15 anchor points.

<DemoBlock2 demo="anchor" engine={props.engine} withThemeSwitcher />

## Popover matches the anchor width
You can force the width of the popper to match the anchor width.

<DemoBlock2 demo="anchor-width" engine={props.engine} withThemeSwitcher />

## Using advanced components

<DemoBlock2 demo="advanced" engine={props.engine} withThemeSwitcher />

# Properties

<PropTable component="Popover" />
