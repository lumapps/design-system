# Popover

## Default behavior
Popover can be used to display elements above the rest of the UI.

<DemoBlock demo="default" engine={props.engine} withThemeSwitcher />

## Offset
Popover can be offseted from its anchor point.

<DemoBlock demo="offset" engine={props.engine} withThemeSwitcher />

## Control the popper visibility

<DemoBlock demo="visibility" engine={props.engine} withThemeSwitcher />

## Using anchor point
Popover can be placed using one of 15 anchor points.

<DemoBlock demo="anchor" engine={props.engine} withThemeSwitcher />

## Popover matches the anchor width
You can force the width of the popper to match the anchor width.

<DemoBlock demo="anchor-width" engine={props.engine} withThemeSwitcher />

## Using advanced components

<DemoBlock demo="advanced" engine={props.engine} withThemeSwitcher />

# Properties

<PropTable component="Popover" />
