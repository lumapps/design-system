---
title: Popover dialog
frameworks: ['react']
---
import * as DemoDefault from './react/default.tsx';
import ReactPopoverDialog from 'lumx-docs:@lumx/react/components/popover-dialog/PopoverDialog';

**Popover dialogs are [Popovers](/product/components/popover) with a [dialog accessibility pattern](https://www.w3.org/WAI/ARIA/apg/example-index/dialog-modal/dialog.html) applied to it.**

<DemoBlock orientation="horizontal" demo={{ react: DemoDefault }} />

### Accessibility concerns

This component should be used when popovers contains complex content with multiple interactive elements.

As this component uses the `dialog` accessible pattern, a few specific behaviors are applied:

-   When the popover is opened, focus will be set on either the element set using the `focusElement` prop or the first focusable element.
    Once the focus is set within the popover, it will be trapped and stay within the popover until it is closed.

-   An accessible name should be provided. Render a [`DialogHeading`](/product/components/dialog#properties) anywhere inside
    the popover dialog to name it automatically (its `role="dialog"` element gets `aria-labelledby` pointing at the rendered
    heading) — see the demo above. Alternatively, set the `label`, `aria-label`, or `aria-labelledby` props directly — an
    explicit `label`/`aria-label`/`aria-labelledby` always takes precedence over an auto-linked `DialogHeading`. `label` is
    optional: when omitted (and no `DialogHeading` is rendered), the popover dialog has no accessible name.

-   The activating element should take focus on popover close (`focusAnchorOnClose` property, activated by default)

## Properties

<PropTable docs={{ react: ReactPopoverDialog }} />
