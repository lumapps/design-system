---
frameworks: ['react']
---
import * as DemoDefault from './react/default.tsx';
import * as DemoWithoutMinAndMaxLabels from './react/without-min-and-max-labels.tsx';
import * as DemoPrecision from './react/precision.tsx';
import * as DemoStep from './react/step.tsx';
import ReactSlider from 'lumx-docs:@lumx/react/components/slider/Slider';

# Slider

**Slider is a visual input where the user can increase or decrease the value between two numeric values.**

<DemoBlock withThemeSwitcher demo={{ react: DemoDefault }} />

## Without min and max labels

Possibility to hide the min and max value.

<DemoBlock withThemeSwitcher demo={{ react: DemoWithoutMinAndMaxLabels }} />

## Precision

Use when the values are decimal numbers.

<DemoBlock withThemeSwitcher demo={{ react: DemoPrecision }} />

## Step

Use to limit the choice between value propositions.

<DemoBlock withThemeSwitcher demo={{ react: DemoStep }} />

### Properties

<PropTable docs={{ react: ReactSlider }} />
