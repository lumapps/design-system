# Slider

## Default

<DemoBlock demo="default" engine={props.engine} disableGrid withThemeSwitcher />

## Without min and max labels

<DemoBlock demo="without-min-max" engine={props.engine} disableGrid withThemeSwitcher />

## Precision

<DemoBlock demo="precision" engine={props.engine} disableGrid withThemeSwitcher />

## Steps

<DemoBlock demo="steps" engine={props.engine} disableGrid withThemeSwitcher />

# Properties

<PropTable component="Slider" />
