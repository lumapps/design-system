# Text field

**Text fields are simple text inputs.**

<DemoBlock demo="default" engine={props.engine} disableGrid withThemeSwitcher />

## States

There are three states: _disabled_, _valid_, and _invalid_.

### Disabled

<DemoBlock demo="disabled" engine={props.engine} disableGrid withThemeSwitcher />

### Valid

<DemoBlock demo="valid" engine={props.engine} disableGrid withThemeSwitcher />

### Invalid

<DemoBlock demo="invalid" engine={props.engine} disableGrid withThemeSwitcher />

## Options

There are five options: _helper text_, _placeholder_, _icon_, _is clearable_ and _with chips_.

### Helper text

<DemoBlock demo="helper" engine={props.engine} disableGrid withThemeSwitcher />

### Placeholder

<DemoBlock demo="placeholder" engine={props.engine} disableGrid withThemeSwitcher />

### Icon

<DemoBlock demo="icon" engine={props.engine} disableGrid withThemeSwitcher />

### Clearable

<DemoBlock demo="clearable" engine={props.engine} disableGrid withThemeSwitcher />

### With Chips

<DemoBlock demo="with-chips" engine={props.engine} disableGrid withThemeSwitcher />

## Text Area

<DemoBlock demo="text-area" engine={props.engine} disableGrid withThemeSwitcher />

### Valid

<DemoBlock demo="text-area-valid" engine={props.engine} disableGrid withThemeSwitcher />

### Invalid

<DemoBlock demo="text-area-invalid" engine={props.engine} disableGrid withThemeSwitcher />

### Properties

<PropTable component="TextField" />
