---
frameworks: ['react', 'vue']
---
import * as ReactDemoDefault from './react/default.tsx';
import * as ReactDemoIsDisabled from './react/is-disabled.tsx';
import * as ReactDemoIsValid from './react/is-valid.tsx';
import * as ReactDemoHasError from './react/has-error.tsx';
import * as ReactDemoIsRequired from './react/is-required.tsx';
import * as ReactDemoHelper from './react/helper.tsx';
import * as ReactDemoPlaceholder from './react/placeholder.tsx';
import * as ReactDemoIcon from './react/icon.tsx';
import * as ReactDemoIsClearable from './react/is-clearable.tsx';
import * as ReactDemoMaxLength from './react/max-length.tsx';
import * as ReactDemoChips from './react/chips.tsx';
import * as ReactDemoTextarea from './react/textarea.tsx';
import * as ReactDemoTextareaValid from './react/textarea-valid.tsx';
import * as ReactDemoTextareaInvalid from './react/textarea-invalid.tsx';
import * as ReactDemoNumber from './react/number.tsx';
import * as VueDemoDefault from './vue/default.vue';
import * as VueDemoIsDisabled from './vue/is-disabled.vue';
import * as VueDemoIsValid from './vue/is-valid.vue';
import * as VueDemoHasError from './vue/has-error.vue';
import * as VueDemoIsRequired from './vue/is-required.vue';
import * as VueDemoHelper from './vue/helper.vue';
import * as VueDemoPlaceholder from './vue/placeholder.vue';
import * as VueDemoIcon from './vue/icon.vue';
import * as VueDemoIsClearable from './vue/is-clearable.vue';
import * as VueDemoMaxLength from './vue/max-length.vue';
import * as VueDemoChips from './vue/chips.vue';
import * as VueDemoTextarea from './vue/textarea.vue';
import * as VueDemoTextareaValid from './vue/textarea-valid.vue';
import * as VueDemoTextareaInvalid from './vue/textarea-invalid.vue';
import * as VueDemoNumber from './vue/number.vue';
import ReactTextField from 'lumx-docs:@lumx/react/components/text-field/TextField';
import VueTextField from 'lumx-docs:@lumx/vue/components/text-field/TextField';

# Text field

**Text fields are simple text inputs.**

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoDefault, vue: VueDemoDefault }} />

## Special states

There are three special states: `isDisabled`, `isValid`, and `hasError`.

### Is disabled

Use `isDisabled` when values can't be edited.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoIsDisabled, vue: VueDemoIsDisabled }} />

### Is valid

Use `isValid` to tell that entered values are considered correct.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoIsValid, vue: VueDemoIsValid }} />

### Has error

Use `hasError` to tell that entered values are considered incorrect.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoHasError, vue: VueDemoHasError }} />

## Options

There are seven options: `isRequired`, `helper`, `placeholder`, `icon`, `clearButtonProps`, `maxLength` and `chips`.

### Is required

Use `isRequired` to tell that the text field must be filled to submit the form.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoIsRequired, vue: VueDemoIsRequired }} />

### Helper

Use `helper` to provide guidance.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoHelper, vue: VueDemoHelper }} />

### Placeholder

Use `placeholder` to give an example of expected values or invite filling text fields.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoPlaceholder, vue: VueDemoPlaceholder }} />

### Icon

Use `icon` to illustrate text fields `label`, `icon` complements `label` but can't replace it.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoIcon, vue: VueDemoIcon }} />

### Is clearable

To allow the user to clear the text field, you would have to pass a `label` prop through `clearButtonProps` prop. This corresponds to the label which will be used inside the tooltip of the clear icon button. Setting the label is enough to determine the visibility of the clear button. Use `onClear` to pass the function triggered by a click on the clear icon button.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoIsClearable, vue: VueDemoIsClearable }} />

### Max length

Use `maxLength` when users inputs length are limited.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoMaxLength, vue: VueDemoMaxLength }} />

### Chips

Use `chips` when users inputs need to be split in distinct parts such as tag entry.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoChips, vue: VueDemoChips }} />

## Textarea

Use Text areas when users are expected to enter more than one line of text.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoTextarea, vue: VueDemoTextarea }} />

### Valid

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoTextareaValid, vue: VueDemoTextareaValid }} />

### Invalid

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoTextareaInvalid, vue: VueDemoTextareaInvalid }} />

## Number

Use number input when users are expected to enter numeric values.

<DemoBlock withThemeSwitcher demo={{ react: ReactDemoNumber, vue: VueDemoNumber }} />

### Accessibility concerns

TextFields use standard HTML input or textarea (when using multiline).

Make sure to always properly label them with the builtin `label` or attaching external labels (via `aria-labelledby`, or
external HTML `label` with `for`).

Showing a text field with only a placeholder is not sufficient, visual and accessible labels must also be in place.

Text fields can be disabled in two ways:

-   `disabled`/`isDisabled` which completely disables the text field. It's not focusable and pointer events (hover, click, etc.) are
    all disabled too. You'll have to find another way to indicate to users that the field is here and why it is disabled.
-   `aria-disabled` only disable editing the text field (read only) so it's still focusable and accessible. You'll have
    to setup either a `helper`, a tooltip or an aria description to explain to the user why this field is disabled.

### Properties

<PropTable docs={{ react: ReactTextField, vue: VueTextField }} />
