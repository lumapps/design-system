---
frameworks: ['react']
---
import * as DemoDefault from './react/default.tsx';
import * as DemoRelevant from './react/relevant.tsx';
import * as DemoThreaded from './react/threaded.tsx';
import ReactCommentBlock from 'lumx-docs:@lumx/react/components/comment-block/CommentBlock';

# Comment block

**Comment blocks display rich text messages as comments or chat messages.**

<DemoBlock withThemeSwitcher demo={{ react: DemoDefault }} />

## Relevant

Use relevant comment block for validated messages as an accepted answer to a question.

<DemoBlock withThemeSwitcher demo={{ react: DemoRelevant }} />

## Threaded

Use threaded comment block to show relation between several messages as replies.

<DemoBlock withThemeSwitcher demo={{ react: DemoThreaded }} />

### Accessibility concerns

Comment blocks and it's default subcomponents do not use any special landmark or accessibility role.

Avoid using the deprecated `onClick`, `onMouseEnter` & `onMouseLeave` props as they do not provide accessible interactive elements.

### Properties

<PropTable docs={{ react: ReactCommentBlock }} />
