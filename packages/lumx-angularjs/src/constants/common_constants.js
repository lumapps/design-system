/**
 * The prefix to use to name the AngularJS component.
 *
 * @type {string}
 * @constant
 * @readonly
 */
const COMPONENT_PREFIX = 'lumx';

/**
 * The name of the AngularJS module.
 * This is the name that is used to add the dependency in the app that uses the package.
 *
 * @type {string}
 * @constant
 * @readonly
 */
const MODULE_NAME = '@lumx/angularjs';

/**
 * The prefix to use to name the AngularJS's services.
 *
 * @type {string}
 * @constant
 * @readonly
 */
const SERVICE_PREFIX = 'LumX';

/////////////////////////////

export { COMPONENT_PREFIX, MODULE_NAME, SERVICE_PREFIX };
