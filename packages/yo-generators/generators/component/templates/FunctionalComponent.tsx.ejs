import classNames from 'classnames';

import {  handleBasicClasses } from '@lumx/core/js/utils/className';
import { forwardRef } from "@lumx/react/utils/react/forwardRef";
import { LumxClassName } from '@lumx/core/js/types';

/**
 * Defines the props of the component.
 */
export interface <%= componentName %>Props {
    /** Children */
    children?: React.ReactNode;
}

/**
 * Component display name.
 */
const COMPONENT_NAME = '<%= componentName %>';

/**
 * Component default class name and class prefix.
 */
const CLASSNAME: LumxClassName<typeof COMPONENT_NAME> = '<%= className %>';

/**
 * Component default props.
 */
const DEFAULT_PROPS: Partial<<%= componentName %>Props> = {
    someProp: 'value',
};

/**
 * [Enter the description of the component here].
 */
export const <%= componentName %> = forwardRef<<%= componentName %>Props, HTMLDivElement>((props, ref) => {
    const { children, className, someProp = DEFAULT_PROPS.someProp, ...forwardedProps } = props;

    return (
        <div
            ref={ref}
            {...forwardedProps}
            className={classNames(
                className,
                handleBasicClasses({
                    prefix: CLASSNAME,
                }),
            )}
        >
            {children}
        </div>
    );
});
<%= componentName %>.displayName = COMPONENT_NAME;
<%= componentName %>.className = CLASSNAME;
<%= componentName %>.defaultProps = DEFAULT_PROPS;
