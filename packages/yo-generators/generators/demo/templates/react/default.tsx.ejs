import React from 'react';

import { <%= componentName %> } from '@lumx/react';

const App = ({ theme }: any) => (
    <>
        <<%= componentName %> theme={theme} />
    </>
);

export default App;
