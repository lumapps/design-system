A Yeoman generator to generate a LumX React component with the associated test file.

Run the following command at the root of the design system project to generate a new React component:

```
yarn scaffold
```
