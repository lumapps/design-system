/* eslint-disable */

const path = require('path');

const ROOT_PATH = path.resolve(__dirname, '../');
const CHANGELOG_PATH = path.resolve(ROOT_PATH, 'CHANGELOG.md');

module.exports = {
    ROOT_PATH,
    CHANGELOG_PATH,
};
